Such a detector itself is probabilistic by default.

What happens when a developer writes code that's too clean? Do they get called into HR for "looking suspiciously AI"?

And if the AI-generated code fixes the bug, well documented, passes all tests, and survives review - are we really supposed to reject it and keep our bug and principles instead?

To me, that's just as dumb as cargo cult. Judge the code by its correctness, maintainability, and test results - not by whether someone thinks it "smells AI"

Linus Torvalds recently expressed what is probably the most sensible and rational position
